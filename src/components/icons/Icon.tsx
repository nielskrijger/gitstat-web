import styled from 'styled-components';

const Icon = styled.div`
  width: 1.1rem;
  height: 1.1rem;
  display: inline-block;
  vertical-align: -0.5rem;
  margin-right: 0.2rem;
`;

export default Icon;
